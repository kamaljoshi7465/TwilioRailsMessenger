<%= form_with(model: messenger) do |form| %>
  <% if messenger.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(messenger.errors.count, "error") %> prohibited this messenger from being saved:</h2>

      <ul>
        <% messenger.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :phone_number, style: "display: block" %>
    <%= form.text_field :phone_number %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
